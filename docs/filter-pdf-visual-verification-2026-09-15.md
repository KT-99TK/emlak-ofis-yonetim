# Filtre ve PDF görsel doğrulaması — 2026-09-15

Masaüstü 1280×720 ve mobil 375×812 görünümlerinde `/contracts`, `/clients`, `/properties` ve gerçek ön muhasebe route’u `/accounting` kontrol edildi. PDF düğmesi görünür; aktif/pasif seçim alanı ve manager danışman kodu alanı taşmadan render oluyor. Mobil görünümde filtreler dikey akışa geçiyor ve metinler okunabilir durumda. `/ledger` gerçek route olmadığı için kontrol `/accounting` üzerinden yapıldı.
